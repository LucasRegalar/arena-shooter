local playerConfig = {
	sprite_size = 20,
	idle_row_y = 20,
	-- skip the last to frames since idle has only 4 / 6,
	idle_frame_trim = 2,
	sprite_offset_y = 1,
	move_speed = 300,
	gamepad_deadzone = 0.2,
	crosshair_max_distance = 60,
	crosshair_radius = 6,
	crosshair_line = 10,
}

return playerConfig


